# 📘 Інструкція для власниці — TryOn

Привіт! 🌸 Тут простими словами розказано, як користуватися своїм ботом:
додавати тренування, керувати тарифами і що робити, якщо в клієнта щось не відкрилося.
**Код знати не треба** — усе робиться кнопками прямо в Telegram.

---

## 🧭 Як це все працює (коротко)

1. Клієнт обирає тариф і **оплачує на сайті**.
2. Сайт автоматично дає йому **персональне посилання** на бота.
3. Клієнт відкриває посилання → бот **сам вмикає доступ** на потрібний строк.
4. Усередині бота клієнт дивиться **твої відео**, розкладені по розділах.

Твоя задача — один раз **наповнити бота відео** і далі лише доглядати за контентом.
Гроші, доступи й строки бот рахує сам.

---

## 🎬 Крок 0. Заливаємо відео на YouTube ПРАВИЛЬНО

Відео живуть на YouTube, а бот їх просто показує. **Найважливіше правило:**

> Відео має бути **«Доступ за посиланням» (Unlisted / Не для всіх)** — НЕ «Приватне».

- **«Доступ за посиланням»** — відео не видно в пошуку й на каналі, але хто має посилання — подивиться. ✅ Те, що треба.
- **«Приватне»** — бачиш тільки ти. Бот не зможе його показати клієнтам. ❌ Не підходить.

**Як виставити при завантаженні:** на YouTube, коли завантажуєш відео, на кроці
«Видимість» обери **«Доступ за посиланням»** і збережи. Усе.

---

## 🛠 Крок 1. Заходимо в панель керування

У чаті з ботом напиши команду:

```
/admin
```

Відкриється **«Адмін-панель»** з трьома кнопками:
- **📂 Розділи каталогу** — папки з відео (курси);
- **👤 Тариф користувача** — змінити тариф клієнту;
- **🧊 Доступ по тарифах** — тимчасово призупинити доступ тарифу.

Це твій пульт керування.

> Команда `/admin` працює тільки з твого акаунта — клієнти її не бачать.

---

## 📁 Крок 2. Створюємо розділ

Розділ — це як папка (наприклад «Пілатес», «Розтяжка», «Харчування»).

1. У `/admin` натисни **📂 Розділи каталогу**, потім **➕ Нова група**.
2. Бот попросить назву — напиши її (наприклад `Пілатес`) і відправ.
3. Готово — розділ з’явився у списку.

Можна зробити **підгрупу** (папку всередині папки): зайди в розділ і натисни **➕ Підгрупа**.

---

## ▶️ Крок 3. Додаємо відео

1. Відкрий **📂 Розділи каталогу** і зайди в потрібний розділ (натисни на нього).
2. Натисни **➕ Відео**.
3. **Скопіюй посилання на відео з YouTube** і відправ боту.
4. Бот сам підтягне назву й картинку-обкладинку. Готово ✨

> Якщо бот напише, що не зміг відкрити відео — майже завжди це тому, що відео
> стоїть «Приватне». Поміняй на YouTube на **«Доступ за посиланням»** і додай ще раз.

---

## 💳 Крок 4. Тарифи — хто що бачить

У тебе три тарифи: **Lite**, **Pro**, **VIP** (від меншого до більшого).

Кожному **розділу** і кожному **відео** можна поставити **мінімальний тариф** —
тобто з якого тарифу його видно. Правило просте:

- поставив розділу **Lite** → бачать **усі** (Lite, Pro, VIP);
- поставив **Pro** → бачать тільки **Pro і VIP**;
- поставив **VIP** → бачать тільки **VIP**.

**Як поставити:** зайди в розділ (або у відео) і натисни одну з кнопок
**`Lite` / `Pro` / `VIP`** у рядку тарифів. Усе, що дешевше — цей розділ не побачить.

> 💡 Приклад: базові тренування постав на `Lite`, бонусні/складніші — на `Pro` або `VIP`.
> Так за одні гроші клієнт бачить більше, за інші — менше, а окремих каталогів робити не треба.

---

## ✏️🗑 Крок 5. Перейменувати або видалити

Усе всередині `/admin`, кнопками:

- **✏️ Назва** — змінити назву розділу чи відео.
- **🗑 Видалити** — прибрати. Якщо видаляєш розділ — разом з ним підуть і його відео
  (бот спитає підтвердження, тож випадково не зітреться).

---

## 🎟 Видати доступ вручну (без оплати)

Іноді треба дати доступ безкоштовно — подрузі, за бартер, як подарунок. Оплата для
цього **не потрібна**.

1. Попроси людину відкрити бота й натиснути **/myid** — бот покаже її **номер (ID)**.
   (Або хай просто напише боту `/start` і скине тобі свій ID.)
2. У себе в чаті з ботом напиши:

   ```
   /grant ID ТАРИФ ДНІ
   ```

   - `ID` — той номер, що прислала людина;
   - `ТАРИФ` — `lite`, `pro` або `vip`;
   - `ДНІ` — на скільки днів (можна не писати — тоді стандартний строк тарифу).

   Приклад: `/grant 123456789 pro 30` — дати тариф Pro на 30 днів.
3. Бот підтвердить. Людина відкриває бота, тисне **/catalog** — і бачить тренування.

---

## 🚫 Закрити доступ достроково

Гроші за курс списуються **один раз** — жодних автоматичних щомісячних списань немає,
тож «відписувати» від оплати нікого не треба. Доступ і так сам згасне в кінці строку.

Але якщо треба **забрати доступ раніше** (повернення грошей, порушення умов) —

```
/revoke ID
```

Приклад: `/revoke 123456789`. Доступ у людини закриється **одразу**.

> 🔎 Перевірити, який зараз доступ у клієнта: `/access ID` — бот покаже тариф і до якої дати діє.

---

## 👤 Змінити тариф клієнту (кнопка в панелі)

У панелі `/admin` є кнопка **«👤 Тариф користувача»**. Нею можна вручну поставити
будь-який тариф — навіть тому, кого ще немає в базі.

1. Тисни **👤 Тариф користувача**.
2. Надішли **ID клієнта** (його номер у Telegram; клієнт дізнається через `/myid`).
3. Бот покаже поточний тариф і кнопки: **Без доступу / Lite / Pro / VIP**.
4. Обери потрібний — готово.

Тариф ставиться на стандартний строк (Lite/Pro — 30 днів, VIP — 90). Кнопка
**«❌ Без доступу»** — просто прибрати доступ. Якщо в людини вже був якийсь тариф,
він заміниться на новий.

---

## 🧊 Тимчасово закрити доступ цілому тарифу (кнопка в панелі)

Кнопка **«🧊 Доступ по тарифах»** в `/admin` — якщо треба на час **призупинити**
тренування для всіх клієнтів якогось тарифу (напр. оновлюєш контент).

- Тисни тариф — він перемкнеться на **🚫 заморожено**. Клієнти цього тарифу побачать
  «доступ тимчасово призупинено» замість тренувань.
- Тисни ще раз — знову **✅ активно**, доступ повернеться.

Важливо: заморозка **не спалює** дні доступу — строк не тікає, поки заморожено.

---

## 👀 Як це бачить клієнт

Клієнт відкриває бота і бачить **«Мої тренування»** — список розділів за своїм тарифом.
Тисне розділ → бачить відео → тисне відео → кнопка **«▶️ Дивитись»** відкриває YouTube.
Знизу завжди показано його **тариф і скільки днів лишилось**.

---

## 🆘 Якщо клієнт каже «я оплатив, а доступу немає»

У 99% випадків це лікується **командою активації**.

На сторінці «Дякуємо за оплату» (куди потрапляє клієнт після оплати) є:
- кнопка **«Перейти в Telegram»**, і
- маленький рядок з командою **`/activate ...`** та кнопкою **«Копіювати»**.

Скажи клієнту:
1. Натиснути **«Копіювати»** під тією командою.
2. Відкрити бота і **вставити та відправити** її.
3. Доступ увімкнеться одразу.

> Чому так буває: якщо людина **раніше вже відкривала бота**, Telegram інколи не передає
> йому код доступу автоматично. Команда `/activate` робить це вручну — і все працює.

Якщо й це не допомогло — напиши розробнику (контакт нижче).

---

## ⌨️ Команди бота (шпаргалка)

| Команда | Що робить |
|---|---|
| `/admin` | Панель: контент, тариф користувача, заморозка тарифів (тільки для тебе) |
| `/grant ID ТАРИФ ДНІ` | Видати доступ вручну без оплати (тільки для тебе) |
| `/revoke ID` | Закрити доступ клієнту достроково (тільки для тебе) |
| `/access ID` | Перевірити доступ клієнта (тільки для тебе) |
| `/start` | Головне меню / каталог |
| `/catalog` | Мої тренування |
| `/myaccess` | Мій тариф і скільки днів лишилось |
| `/myid` | Дізнатись свій номер (ID) — щоб отримати доступ вручну |
| `/activate` | Увімкнути доступ за кодом (для клієнтів) |
| `/help` | Підказка зі списком команд |

---

## ☎️ Якщо щось зламалося

Спершу спробуй просто **зачекати пару хвилин і повторити** — інколи це тимчасове.

Якщо не минає (бот не відповідає, відео не додається, клієнти не отримують доступ) —
**напиши розробнику** й по можливості додай:
- що саме робила (яка кнопка / команда),
- що відповів бот (скріншот дуже допомагає),
- о котрій годині це сталося.

Так проблему знайдуть і виправлять набагато швидше. 💛
